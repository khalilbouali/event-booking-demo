<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>fr.carrefour</groupId>
	<artifactId>reservation-ms</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>reservation-ms</name>
	<description>The app handling user reservations.</description>

	<developers>
		<developer>
			<id>khalil.bouali</id>
			<name>Khalil Bouali</name>
			<email>khalil.bouali95@gmail.com</email>
			<roles>
				<role>Developer</role>
			</roles>
			<timezone>+1</timezone>
		</developer>
	</developers>

	<properties>
		<java.version>25</java.version>
		<jacoco.version>0.8.15</jacoco.version>
	</properties>

	<dependencies>

		<!-- Reactive REST API -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>

		<!-- Reactive MongoDB -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb-reactive</artifactId>
		</dependency>

		<!-- Validation -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- Keycloak JWT / OAuth2 Resource Server -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>

		<!-- Kafka -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka</artifactId>
		</dependency>

		<!-- Actuator -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
		</dependency>

		<!-- Tests -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-webflux-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-kafka</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-data-mongodb-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-mongodb</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>

			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>

			<!-- Required for Lombok annotation processing with Java 25 -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>

				<executions>

					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>

					<execution>
						<id>jacoco-report</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>

						<configuration>
							<excludes>
								<exclude>**/*Application.class</exclude>

								<exclude>**/application/port/**</exclude>
								<exclude>**/application/model/EventSeatCandidates.class</exclude>

								<exclude>**/domain/exception/**</exclude>

								<exclude>**/configuration/ReservationConfiguration.class</exclude>
								<exclude>**/configuration/ReservationProperties.class</exclude>

								<exclude>**/infrastructure/adapter/in/web/dto/**</exclude>

								<exclude>**/infrastructure/adapter/in/kafka/event/**</exclude>
								<exclude>**/infrastructure/adapter/in/kafka/exception/**</exclude>

								<exclude>**/infrastructure/adapter/out/event/dto/**</exclude>

								<exclude>**/infrastructure/adapter/out/kafka/event/**</exclude>

								<exclude>**/infrastructure/adapter/out/persistence/mongodb/document/**</exclude>
								<exclude>**/infrastructure/adapter/out/persistence/mongodb/repository/**</exclude>
							</excludes>
						</configuration>
					</execution>

					<execution>
						<id>jacoco-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>

						<configuration>

							<excludes>
								<exclude>**/*Application.class</exclude>

								<exclude>**/application/port/**</exclude>
								<exclude>**/application/model/EventSeatCandidates.class</exclude>

								<exclude>**/domain/exception/**</exclude>

								<exclude>**/configuration/ReservationConfiguration.class</exclude>
								<exclude>**/configuration/ReservationProperties.class</exclude>

								<exclude>**/infrastructure/adapter/in/web/dto/**</exclude>

								<exclude>**/infrastructure/adapter/in/kafka/event/**</exclude>
								<exclude>**/infrastructure/adapter/in/kafka/exception/**</exclude>

								<exclude>**/infrastructure/adapter/out/event/dto/**</exclude>

								<exclude>**/infrastructure/adapter/out/kafka/event/**</exclude>

								<exclude>**/infrastructure/adapter/out/persistence/mongodb/document/**</exclude>
								<exclude>**/infrastructure/adapter/out/persistence/mongodb/repository/**</exclude>
							</excludes>

							<rules>
								<rule>
									<element>BUNDLE</element>

									<limits>

										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.80</minimum>
										</limit>

										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.70</minimum>
										</limit>

									</limits>
								</rule>
							</rules>

						</configuration>
					</execution>

				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>

				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>

</project>
